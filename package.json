{
  "name": "my-url-shortener",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.713.0",
    "express": "^4.17.1"
  }
}
